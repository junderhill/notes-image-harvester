'use strict'

const expect = require('chai').expect

describe('Processor module', () =>{
    describe('processNote', ()=>{
        describe('when the note contains no images', () => {
            
        });

    });

});